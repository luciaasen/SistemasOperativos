" vim configuration

"display line number
set number

"case insensitive for find
set ignorecase

"spaces instead of tabs
set expandtab

" 1 tab = 4 spaces
set shiftwidth=4
set tabstop=4

"Auto indentation
set autoindent

"When splitted buffers, use the mouse to move the middle '---' line
set mouse=n
set ttymouse=xterm2
